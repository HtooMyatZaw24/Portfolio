import React from "react";
import { Box, Text } from "@radix-ui/themes";
const Footer = () => {
  return (
    <Box className="" style={{ boxShadow: "0px -5px 5px rgba(0, 0, 0, 0.1)" }}>
      <Text className="text-white text-4xl">Footer</Text>
    </Box>
  );
};

export default Footer;
